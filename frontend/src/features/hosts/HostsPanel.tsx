import { UiCard } from "../../components/UiCard";

export function HostsPanel() {
  return (
    <UiCard title="Hosts / Sites">
      <p className="muted">Per-vhost summaries and filters will appear here.</p>
    </UiCard>
  );
}
